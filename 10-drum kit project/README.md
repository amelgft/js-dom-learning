# Key points
  a funtion with no name , is an anonymous function

  the eventListener took event name as a string paramether and the function name only without
  parenthese

  - we can specify a function in two way in an event listener, one as anonymous , second as separate function
  